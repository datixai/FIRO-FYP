/**
 * script.js
 * ─────────────────────────────────────────────────────────────
 * Firebase is now initialised via firebase-init.js which reads
 * config from the app.py backend (/api/firebase-config).
 *
 * Import pattern used in all dashboard pages:
 *
 *   import { initFirebase } from "./firebase-init.js";
 *   const { app, auth, db, config } = await initFirebase();
 *
 * This file is kept for reference / legacy compatibility.
 * ─────────────────────────────────────────────────────────────
 */
